# Dependencias directas de runtime fijadas para builds reproducibles. El
# Dockerfile instala este archivo; requirements.txt es el entorno de desarrollo
# de Windows y no se usa en Railway.
# 6.0.8 cierra CVE-2026-15830 (recursión en django.contrib.gis.geos). No era
# explotable aquí —la app no instala contrib.gis— pero `pip-audit`, que desde
# el 07/09/2026 corre en la CI, lo reportaría en cada corrida. Ficha D16.
Django==6.0.8
psycopg2-binary==2.9.12
python-decouple==3.8
twilio==9.10.9
django-axes==7.0.1
redis==8.0.1
requests==2.33.0
# Servidor WSGI de producción y servidor de estáticos (despliegue, 04/07/2026)
gunicorn==26.0.0
whitenoise==6.12.0
