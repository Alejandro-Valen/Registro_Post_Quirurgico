# Dependencias directas de runtime — instalar en producción.
# Para reproducir el entorno exacto de desarrollo usar requirements.txt (pip freeze completo).
Django==6.0.6
psycopg2-binary>=2.9
python-decouple>=3.8
twilio>=9.10
django-axes==7.0.1
